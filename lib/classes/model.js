'use strict';

const Document = require('camo').Document;

exports = module.exports = (function () {
  // @todo: Add method to add attributes to model

  class Model extends Document {
    constructor() {
      super();
      // @todo
    }
  }

  return Model;
}());