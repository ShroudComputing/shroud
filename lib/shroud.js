'use strict';

exports = module.exports = (function () {
  class Shroud {
    constructor(config) {
      this._config = config || {};
      // @todo
    }

    start() {
      // @todo
      return this;
    }
  }

  return Shroud;
}());